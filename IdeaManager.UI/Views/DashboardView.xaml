<Page x:Class="IdeaManager.UI.Views.DashboardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:IdeaManager.UI.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DashboardView">

    <Grid>
        <StackPanel Margin="20" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Dashboard" FontSize="25" FontStyle="Oblique" FontWeight="DemiBold" Margin="0 0 0 20"/>

            <Button Content="Submit an idea"
                    Width="200" Height="40"
                    Margin="0 10"
                    Click="OnSubmitIdeaClick"
                    Background="Pink"
                    />

            <Button Content="See all ideas"
                    Width="200" Height="40"
                    Margin="0 10"
                    Click="OnViewIdeasClick"
                    Background="LightPink"
                    />

            <Button Content="See all projects"
                    Width="200" Height="40"
                    Margin="0 10"
                    Click="OnViewProjectsClick"
                    Background="HotPink"
                    />
        </StackPanel>
    </Grid>
</Page>